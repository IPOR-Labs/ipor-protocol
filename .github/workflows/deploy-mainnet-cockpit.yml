name: Deploy mainnet cockpit

on:
  workflow_dispatch:
    inputs:
      cockpit-app-name:
        description: 'IPOR cockpit Amplify application name'
        required: true
        default: 'ipor-mainnet-cockpit'
        type: choice
        options:
          - ipor-mainnet-cockpit

jobs:
  notify:
    uses: ./.github/workflows/notify-slack.yml
    secrets:
      SLACK_BOT_TOKEN: ${{ secrets.MAINNET_SLACK_BOT_TOKEN }}
    with:
      status-slack-channel-name: github-mainnet-ci

  deploy:
    needs:
      - notify
    uses: ./.github/workflows/deploy-amplify.yml
    with:
      aws-region: eu-central-1
      app-name: ${{ github.event.inputs.cockpit-app-name }}
      branch: ${{ needs.notify.outputs.branch-name }}
    secrets:
      AWS_ACCESS_KEY_ID: ${{ secrets.MAINNET_AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.MAINNET_AWS_SECRET_ACCESS_KEY }}

  report:
    if: ${{ always() }}
    needs:
      - notify
      - deploy
    uses: ./.github/workflows/report-slack.yml
    secrets:
      SLACK_BOT_TOKEN: ${{ secrets.MAINNET_SLACK_BOT_TOKEN }}
    with:
      success: ${{ needs.deploy.result == 'success' }}
      slack-status-msg-id: ${{ needs.notify.outputs.slack-status-msg-id }}
      status-slack-channel-name: github-mainnet-ci
      alarms-slack-channel-name: alarms-mainnet-github
