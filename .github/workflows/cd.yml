name: CD

on:
  push:
    branches:
      - main
      - develop

jobs:

  build-sc:
    needs: notify
    uses: ./.github/workflows/build-smart-contracts.yml
    secrets:
      HARDHAT_FORKING_URL: ${{ secrets.HARDHAT_FORKING_URL }}

